# ===================================================================
# Configuration Tests Mila-Assist
# ===================================================================
# IMPORTANT: Ce fichier est un exemple. Le vrai .env est dans deploy-nas/
# Les scripts de test chargent automatiquement deploy-nas/.env
# ===================================================================

# URL de l'API (via reverse proxy HTTPS ou HTTP direct)
API_URL=https://votre-nas.synology.me:10443
# ou
# API_URL=http://votre-nas.synology.me:9000

# Configuration MySQL (optionnel, chargé depuis deploy-nas/.env)
MYSQL_HOST=votre-nas.synology.me
MYSQL_PORT=3306
MYSQL_USER=mila_user
MYSQL_PASSWORD=votre_mot_de_passe_mysql
MYSQL_DATABASE=mila_assist_db

# CORS Origins (utilisé pour détecter l'URL API)
CORS_ORIGINS=https://votre-nas.synology.me:10443,http://localhost:3000

# ===================================================================
# NOTE: Les scripts de test utilisent deploy-nas/.env par défaut
# Ce fichier .env.example est fourni à titre documentaire uniquement
# ===================================================================
